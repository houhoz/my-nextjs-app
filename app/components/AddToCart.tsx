'use client';

import React from 'react';

const AddToCard = () => {
  return (
    <button
      className="btn btn-primary"
      onClick={() => console.log('111', 111)}
    >
      Add To Cart
    </button>
  );
};

export default AddToCard;
