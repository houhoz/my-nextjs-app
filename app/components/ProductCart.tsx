import React from 'react';
import AddToCard from './AddToCart';

const ProductCard = () => {
  return (
    <div>
      ProductCard
      <AddToCard />
    </div>
  );
};

export default ProductCard;
